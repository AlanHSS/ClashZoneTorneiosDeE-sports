import { PlataformNamePipe } from './plataform-name-pipe';

describe('PlataformNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PlataformNamePipe();
    expect(pipe).toBeTruthy();
  });
});
